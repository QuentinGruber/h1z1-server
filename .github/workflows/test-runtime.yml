name: test runtime
on:
  push:
    branches: [master]
  pull_request:
    branches: [master, dev]
jobs:
  test-linux:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [14.x, 16.5.0, 16.x]
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm i
      - run: npm test
  test-win:
    runs-on: windows-latest
    strategy:
      matrix:
        node-version: [14.x, 16.5.0, 16.x]
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm i
      - run: npm test
